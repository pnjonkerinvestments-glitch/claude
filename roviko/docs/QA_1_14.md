# QA voor Roviko 1.14.0 (24 september 2026)

## Uitgevoerd

| Controle | Resultaat |
| --- | --- |
| `npm run typecheck` | geslaagd |
| `npm run build` | geslaagd |
| `npm test` | **121 van 121 geslaagd** |
| ESLint, hele repo | 272 fouten en 166 waarschuwingen, tegenover 281 fouten en 179 waarschuwingen vóór 1.14; de nieuwe bestanden hebben alleen de `any`-typen die uit `RovikoApp` zijn verhuisd |
| Contrast (`tests/atelier.test.mjs`, nu inclusief `design.css`) | alle paren minstens 4,5:1 in licht en donker; zie `.test-runtime/contrast.json` |
| Horizontaal scrollen | geen, op 320, 375, 430, 768, 1024, 1280 en 1440 px, voor `/`, `/daily`, `/explore`, `/leaderboard`, `/profile`, `/multiplayer`, `/how-to-play`, `/scoring`, `/privacy` en `/sources` |
| Door gespeeld (headless Chromium) | een volledige Rank Radar, met viering, dagdoelen en homepage-route bijgewerkt; World Trip en Wereldduel geopend; kamer gemaakt en lobby geopend; onbekende kamer geeft "We kunnen die kamer niet vinden" |
| Talen en thema's | EN, NL en ES; licht en donker (mobiel donker gecontroleerd) |
| Visuele regressieset | `docs/screenshots/`: home, ontdekken, ranglijst en paspoort (desktop en mobiel), spel, lobby, en home in donker op mobiel |

## Niet uitgevoerd (open)

- Echte iOS- en Android-toestellen: de safe area van de tabbalk, het toetsenbord bij het codeveld en `navigator.share`.
- Schermlezers: VoiceOver, TalkBack en NVDA. De structuur (landmarks, tabs, `aria-live`) is wel in de code gecontroleerd.
- Een multiplayermatch met meerdere echte spelers na de redesign. De multiplayer-integratietest in `npm test` slaagt wel.
- Een geautomatiseerde pixelvergelijking in CI.
