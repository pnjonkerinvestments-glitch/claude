# Rank Radar release 1.9 — 2026-09-22

## Executed

- TypeScript check passed.
- Official Sites production build passed, including client, server and SSR output.
- Full automated suite: **75 passed, 0 failed**.
- Ranking tests cover competition ties, missing observations, coverage normalization, units and spoiler-free sharing. Across 180 deterministic seeds, each set has six distinct countries, four distinct subjects per country, six distinct winning subjects and a clear winning margin. All 15 subjects appear as winners.
- Component tests confirm immediate red incorrect-choice feedback, the green correct choice, written explanations, duplicate-click locking, next-step blocking until acknowledgement, restoration on reload and reconciliation after a lost response. A failed unsaved guess requires explicit recovery.
- D1/Workerd integration checks verify two guests receive identical daily content, session ownership, rejection through other game endpoints, invalid guesses, concurrent-answer locking, six-round completion, immutable daily results, no points/XP, personal statistics/stamps and fresh practice. No future question list is sent during play; the complete review appears after completion. Current-question solutions are intentionally exposed only in this unranked learning game.
- Existing tests for all six solo modes, comparisons, Mosaic, account flows and multiplayer passed. A pre-existing timing-sensitive reconnect test initially failed under parallel load; it now awaits the bounded observable terminal state instead of assuming all retries finish within 50 milliseconds. Production reconnect behavior was not changed.
- SSR output contains four daily cards, with Rank Radar first, plus the six practice modes. English/Dutch translation keys remain aligned. Decorative cover derivatives exist and remain below 40 KB each. Existing light/dark contrast checks pass.
- Native artwork regenerated, deterministic Xcode project regenerated, and portable package validation passed: project references, two test schemes, source delimiters, plists, all four cover images, 195 flags, licences, geography and the opaque 1024px app icon.
- Source review checked native route/version selection, immediate feedback and restored wrong answers, backend response fields, sourced recaps, small-screen text wrapping, reduced-motion rules, source attribution and private-route noindex metadata.

## Not executed

The supervised preview service could not start because its daemon mailbox was unavailable. No new browser screenshots, measured viewport/overflow checks, keyboard E2E, VoiceOver or mobile touch sessions were performed. SSR/component/CSS checks are not a substitute. The deployed website was not used as a fallback preview.

This environment has no Swift compiler, Xcode, iOS SDK, simulator or Apple Developer identity. The native source has **not** been compiled/type-checked, its XCTest/XCUITest suites have **not** run, and no IPA, signed archive or App Store submission was produced. Portable checks only verify source/resource packaging. Run `ios/scripts/test-on-mac.sh`, real-device QA and the signing/archive process before TestFlight. Do not describe this source package as an App Store-ready binary.

## Remaining focused release checks

On a supported browser/device, check the four daily cards and Rank Radar at 320, 375, 390, 430, 768, 1366 and 1920 pixels; both themes and both languages; keyboard focus, 1–4 shortcuts, text zoom and reduced motion. Complete the game, reload a red answer, disconnect during saving, finish a daily, return to the homepage and open fresh practice. On iPhone, additionally test accessibility text sizes and VoiceOver, then complete the same daily in the website and the signed-in native client.

Working brand names, privacy/terms and owner/support information retain their existing commercial-release review requirements. No new legal clearance or compliance claim is made.
