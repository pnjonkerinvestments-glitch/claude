# Roviko 1.7 verification

Checked 2026-09-21.

- Production build: passed with the Sites build helper (vinext 0.0.50 / Vite 8.0.13).
- TypeScript: `npm run typecheck` passed.
- Full automated suite: `npm test` — **65 passed, 0 failed**, 20.84 seconds.
- `git diff --check`: passed.
- The real isolated Workerd/D1 integration tests exercise solo games, accounts, saved results, daily immutability, private WebSocket rooms, answer authority, reconnect and a 12-player rematch. No test records were written to production.
- New checks cover all 40 curated country records, reachability of both clue variants, both translations, consistent downloadable metadata, four complete groups, attribution, no country-name answer in clue text, same-continent distractors and catalogue fallback for legacy targeted practice.
- UI component tests verify that stories and source links appear only for solved countries, missing legacy metadata is safe, incorrect tiles retain red feedback, duplicate saves lock and save failures preserve selections.
- Server-rendered homepage and emitted production Mosaic CSS checks pass. All six local cover images exist and remain below 40 KB each; the decorative images have empty alternative text and responsive size candidates.
- Theme contrast checks cover body text, muted text, primary actions, red feedback and the three artwork background colours in both themes. Lowest measured ratio: **5.2:1** (light / muted). This is a token-level check, not a claim of a complete accessibility audit.

## Remaining visual check

The supervised Sites preview service could not start because its preview daemon mailbox was unavailable. Following the preview troubleshooting flow, one status check confirmed the same infrastructure condition. No alternate server or production URL was used as a preview. **Browser screenshots, touch/device interaction and visual overflow checks were not performed for this release.** Responsive source rules, production CSS and semantic UI output were checked, but they do not replace those browser checks. Review at 320, 375, 390, 430, 768, 1366 and 1920px, both themes, once the preview service is available.

## Content continuity

New practice boards use the catalogue now. Already published daily snapshots and saved sessions remain unchanged. Future daily snapshots use the new dataset version. There is no migration or account reset.
